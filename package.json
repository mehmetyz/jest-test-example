{
  "name": "jest_test_example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm -g install chromedriver --unsafe-perm=true --allow-root && jest --ci --reporters=buildkite-test-collector/jest/reporter"
  },
  "jest": {
    "reporters": [
      "default",
      "buildkite-test-collector/jest/reporter"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "buildkite-test-collector": "^1.0.1",
    "jest": "^27.5.1",
    "node-fetch": "^3.2.6",
    "selenium-webdriver": "^4.2.0"
  },
  "devDependencies": {
    "jest-json-reporter": "^1.2.2",
    "jest-junit": "^13.2.0"
  }
}
