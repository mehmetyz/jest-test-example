{
  "name": "jest_test_example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --ci --reporters=default --reporters=jest-junit --outputFile=./test-results.xml"
  },
  "jest": {
    "reporters": [
      "default",
      "jest-junit"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "buildkite-test-collector": "^1.0.1",
    "jest": "^27.5.1",
    "jest-unit": "0.0.1",
    "node-fetch": "^3.2.6",
    "selenium-webdriver": "^4.2.0"
  },
  "devDependencies": {
    "jest-json-reporter": "^1.2.2",
    "jest-junit": "^13.2.0"
  }
}
